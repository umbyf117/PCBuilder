<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/red_dark</item>
        <item name="colorPrimaryVariant">@color/red_light</item>
        <item name="android:textColor">@color/text_night</item>
        <item name="background">@color/background_night</item>
        <item name="backgroundColor">@color/background_night2</item>
        <item name="android:strokeColor">@color/gold_night</item>
    </style>
</resources>